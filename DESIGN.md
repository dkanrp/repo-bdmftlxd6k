# DESIGN.md — 전자기능사 필기 완성노트

이 파일은 이 노트의 시각 언어를 **토큰 · 규칙 · 이유**를 한곳에 적어 정의합니다. 새 화면이나 요소를 추가할 때 여기 적힌 값에서 고르고, 여기에 없는 상황을 만나면 "이유" 항목을 근거로 판단합니다.

> 형식은 `DESIGN.md` 관례를 따릅니다 — 토큰만 나열한 Figma 내보내기(무엇만 있고 왜가 없음)와 사람을 위한 브랜드 가이드(너무 느슨함) 사이를 메우는 것이 목적입니다.

---

## 1. 성격

**종이 위의 공학 노트.** 화려한 앱이 아니라, 시험 직전에 조용히 들여다보는 필기 노트를 지향합니다.

- 크림색 종이 바탕 + 짙은 잉크색 글자 — 장시간 읽어도 눈이 덜 피로하도록
- 장식적 그라데이션·그림자 남용 없음. 그림자는 종이가 살짝 떠 있는 정도만
- 강조는 색을 넓게 칠하는 대신 **왼쪽 테두리 3px**, **작은 알약(pill)**, **얇은 점선 구분** 으로
- 수식·숫자·기호는 반드시 고정폭 서체로 — 자릿수와 첨자가 흐트러지면 오답의 원인이 됨

## 2. 색 토큰

라이트/다크 두 벌을 CSS 변수로 정의하며, **하드코딩된 색상값을 새로 쓰지 않습니다.**

| 토큰 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| `--paper` | `#f5f2e9` | `#10140f` | 페이지 바탕 |
| `--surface` | `#ffffff` | `#171d17` | 카드·표·예제 상자 |
| `--surface-2` | `#ece6d6` | `#1e251c` | 표 머리글, 보조 칩 |
| `--ink` | `#1c221c` | `#e7ece3` | 본문 글자 |
| `--ink-soft` | `#4c5a4c` | `#a7b3a2` | 보조 설명, 캡션 |
| `--line` | `#dcd4bd` | `#2c352a` | 테두리·구분선 |
| `--accent` | `#1f6f4a` | `#57c98a` | 주 강조(녹색) — 링크, 활성 상태 |
| `--accent-strong` | `#154f34` | `#8fe0b3` | 제목·강조 글자 |
| `--accent-soft` | `#e2eee3` | `rgba(87,201,138,.14)` | 강조 배경 |
| `--accent-2` | `#a8571f` | `#e2954f` | 보조 강조(주황) — 기호·라벨 |
| `--accent-3` | `#6a4c9c` | `#a58ce0` | 3차 강조(보라) — 분류 태그 |
| `--danger` | `#a13f2f` | `#e2725c` | 오답·경고 |
| `--diff-low` | `#2f6f91` | `#7fc4e8` | 난이도 "하", 정보성 표시 |
| `--highlight-bg` | `#fbe9a8` | `rgba(226,149,79,.28)` | 이론 위치 강조(사용자 설정 가능) |

**규칙**
- 새 색이 필요하면 먼저 위에서 찾습니다. 정말 없으면 토큰으로 **추가**하고 다크값을 반드시 같이 정의합니다.
- 본문 대비 4.5:1, 큰 글자·UI 요소 3:1 이상(WCAG 2.2 AA). 라이트·다크 **양쪽** 다 확인합니다.
- 의미로 고릅니다. 녹색은 "맞음"이 아니라 **주 강조**이고, 정답 표시는 난이도/상태 전용 토큰을 씁니다.

**이유:** 색을 의미에 묶어두면 다크 모드를 만들 때 색을 하나씩 뒤집을 필요가 없고, 이 노트처럼 한 파일이 계속 자라는 구조에서 색이 제멋대로 번지는 것을 막습니다.

## 3. 타이포그래피

- 본문: **IBM Plex Sans KR** (400 / 600 / 700)
- 수식·숫자·코드·라벨: **IBM Plex Mono** (400 / 600 / 700)
- 두 서체 모두 사용 글자만 서브셋해 파일에 내장 — CDN 요청 없음, 오프라인에서도 동일하게 표시 (SIL OFL 1.1)
- 본문 줄 간격 1.65 — 한글 조판에서 읽기 편한 값
- 굵기는 400·600·700만 사용합니다. 500은 쓰지 않습니다.

**규칙**: 글자 크기는 `rem`으로만 지정합니다. 설정의 글자 크기 조절이 `html`의 `font-size`를 바꾸는 방식이라, `px`로 적으면 그 요소만 확대에서 빠집니다.

**이유:** 사용자가 85~140%까지 글자 크기를 바꿀 수 있고, 수식·분수까지 같이 커져야 의미가 있습니다.

## 4. 간격 · 모서리 · 그림자

- 간격은 `rem` 기준, 대략 `.3 / .5 / .7 / .9 / 1.1 / 1.4 / 2` 단계를 씁니다.
- 모서리: **8px**(카드·표·예제 상자), **6px**(작은 입력·버튼), **999px**(알약·칩). 그 외 값은 쓰지 않습니다.
- 그림자는 `--shadow` 하나만 사용합니다.

## 5. 도형(회로도)

- 인라인 SVG로만 그립니다. 이미지 파일을 쓰지 않습니다 — 단일 HTML 파일 유지와 다크 모드 대응 때문입니다.
- 선·글자 색은 반드시 `currentColor` 또는 `var(--…)`. 다크 모드에서 사라지는 것을 막기 위함입니다.
- `viewBox`를 항상 넣고, 폭은 `max-width:100%`로 줄어들 수 있게 합니다.
- SVG 안 글자는 가장자리에서 **15px 이상** 띄웁니다. `text-anchor="middle"`은 글자 폭의 절반이 양옆으로 번집니다.
- 좁은 화면에서 작아지므로 모든 도형은 **확대해서 보기**를 지원합니다.

**이유:** 회로도는 이 노트의 핵심 자산인데, 과거에 고정 폭 때문에 잘리거나 라벨이 겹쳐 여러 번 다시 그린 적이 있습니다.

## 6. 조작 요소

- 모든 조작 요소는 **최소 24×24 CSS px** (WCAG 2.2 SC 2.5.8). 본문 속 인라인 링크는 예외.
- 클릭 가능한 것은 `<button>`으로 만듭니다. `<div onclick>` 금지.
- 아이콘만 있는 버튼에는 `aria-label`을 답니다.
- 화면 갱신 없이 바뀌는 상태(토스트, 답변 수, 검색 결과 수)는 `aria-live`로 알립니다.

## 7. 알려진 불일치 (정리 대상)

여기 적힌 기준으로 현재 상태를 점검한 결과입니다.

1. **앱 아이콘·`theme-color`가 본문 색과 다릅니다.** 노트의 주 강조는 녹색(`#1f6f4a`)인데 아이콘 배경과 `theme-color`는 파란색(`#2f6f91`, 원래 `--diff-low` 값)입니다. 브랜드 일관성 관점에서 아이콘을 녹색 계열로 맞추는 것이 맞습니다. (아이콘 변경은 APK 재빌드가 필요합니다.)
2. **글자 크기 단계가 없습니다.** 현재 `.66 / .68 / .7 / .74 / .76 / .78 / .8 / .82 / .85 / .86 / .88 / .9 / .92 / 1rem` 등 14종 이상이 제각각 쓰이고 있습니다. `.74 / .8 / .86 / .92 / 1 / 1.15rem` 정도의 단계로 정리하면 화면이 한결 정돈됩니다.
3. **모서리 값 이탈:** `3px`, `7px`, `10px`, `12px`가 일부 남아 있습니다. 8/6/999 중 하나로 맞춥니다.
